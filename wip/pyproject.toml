[project]
name = "routine_wip"
version = "0.1.0"
description = "Automasi scraping data harian WIP"
rcequires-python = ">=3.8"

dependencies = ["selenium", "python-dotenv", "pywin32", "pandas"]

[tool.setuptools]
packages = ["src"]
